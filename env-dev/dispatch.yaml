appName: dispatch
resources:
      requests:
        memory: "64Mi"
        cpu: "30m"
      limits: 
        memory: "128Mi"
        cpu: "100m"

probes:
  liveness:
    initialDelaySeconds: 30
  readiness:
    initialDelaySeconds: 30